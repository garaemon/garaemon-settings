# __PROJECT_NAME__

__PROJECT_DESCRIPTION__

## Development

This project uses [pre-commit](https://pre-commit.com) to run linters and
formatters before each commit. Install the hooks once after cloning:

```bash
pre-commit install
```

Run all checks manually against every file:

```bash
pre-commit run --all-files
```

<!-- Language-specific setup, build, and test instructions are appended by
     the project-init skill for the chosen language. -->
