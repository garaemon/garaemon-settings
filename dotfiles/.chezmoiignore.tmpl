LICENSE
README.md
tests
scripts
install.sh
{{ if ne .chezmoi.os "darwin" }}
dot_config/private_karabiner
private_Library
{{ end }}
# Every path in this file is skipped, so the block below installs the Slack
# digest timers on ax8-max alone. No other machine runs the digests, and
# chezmoi strips the domain suffix, so ax8-max.local reports "ax8-max".
{{ if ne .chezmoi.hostname "ax8-max" }}
dot_config/systemd
.chezmoiscripts/run_onchange_after_enable-slack-digest-timers.sh.tmpl
{{ end }}
