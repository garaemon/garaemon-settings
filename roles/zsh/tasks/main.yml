- name: Copy for zshrc
  copy:
    src: "{{ansible_env.HOME}}/gprog/garaemon-settings/roles/zsh/files/zshrc"
    dest: ~/.zshrc
- name: Copy for zlogin
  copy:
    src: "{{ansible_env.HOME}}/gprog/garaemon-settings/roles/zsh/files/zlogin"
    dest: ~/.zlogin
- name: Copy for zlogout
  copy:
    src: "{{ansible_env.HOME}}/gprog/garaemon-settings/roles/zsh/files/zlogout"
    dest: ~/.zlogout
- name: Copy for zprofile
  copy:
    src: "{{ansible_env.HOME}}/gprog/garaemon-settings/roles/zsh/files/zprofile"
    dest: ~/.zprofile
- name: Copy for zshenv
  copy:
    src: "{{ansible_env.HOME}}/gprog/garaemon-settings/roles/zsh/files/zshenv"
    dest: ~/.zshenv
- name: Copy for zshrc.zplug
  copy:
    src: "{{ansible_env.HOME}}/gprog/garaemon-settings/roles/zsh/files/zshrc.zplug"
    dest: ~/.zshrc.zplug
