- name: Symlink for zshrc
  file:
    src: "{{ansible_env.PWD}}/resources/rcfiles/zshrc"
    dest: ~/.zshrc
    state: link
- name: Symlink for zlogin
  file:
    src: "{{ansible_env.PWD}}/resources/rcfiles/zlogin"
    dest: ~/.zlogin
    state: link
- name: Symlink for zlogout
  file:
    src: "{{ansible_env.PWD}}/resources/rcfiles/zlogout"
    dest: ~/.zlogout
    state: link
- name: Symlink for zpreztorc
  file:
    src: "{{ansible_env.PWD}}/resources/rcfiles/zpreztorc"
    dest: ~/.zpreztorc
    state: link
- name: Symlink for zprofile
  file:
    src: "{{ansible_env.PWD}}/resources/rcfiles/zprofile"
    dest: ~/.zprofile
    state: link
- name: Symlink for zshenv
  file:
    src: "{{ansible_env.PWD}}/resources/rcfiles/zshenv"
    dest: ~/.zshenv
    state: link
- name: Symlink for zshrc.zplug
  file:
    src: "{{ansible_env.PWD}}/resources/rcfiles/zshrc.zplug"
    dest: ~/.zshrc.zplug
    state: link
