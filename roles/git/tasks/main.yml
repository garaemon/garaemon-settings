- name: git config core.page
  ansible.builtin.command: git config core.pager "diff-so-fancy | less --tabs=4 -RFX"
- name: git config alias.see
  ansible.builtin.command: git config alias.see browse
- name: git config alias.p
  ansible.builtin.command: git config alias.p pull-request
- name: git config alilas.br
  ansible.builtin.command: git config alias.br branch
- name: git config alias.co
  ansible.builtin.command: git config alias.co checkout
- name: git config alias.st
  ansible.builtin.command: git config alias.st status
- name: git config alias.unstage
  ansible.builtin.command: git config alias.unstage "reset -q HEAD --"
- name: git config alias.uncommit
  ansible.builtin.command: git config alias.uncommit "reset --mixed HEAD~"
- name: git config alias.graph
  ansible.builtin.command: git config alias.graph "log --graph -10 --branches --remotes --tags  --format=format:'%Cgreen%h %Creset• %<(75,trunc)%s (%cN, %cr) %Cred%d' --date-order"
- name: git config alias.precommit
  ansible.builtin.command: git config alias.precommit "diff --cached --diff-algorithm=minimal -w"
- name: git config alias.unmerged
  ansible.builtin.command: git config alias.unmerged "diff --name-only --diff-filter=U"
- name: git config alias.remotes
  ansible.builtin.command: git config alias.remotes "remotes = remote -v"
- name: git config rebase.autostash
  ansible.builtin.command: git config rebase.autostash true
